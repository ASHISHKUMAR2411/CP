// CPP program to find the revrerse of the string in the quickest way 
// in this we will have the reverse string function
#include<iostream>
#include<string>
#include<vector>
using namespace std;
int main()
{
    string str1;
    cin>>str1;
    Reverse(str);
    return 0;
}