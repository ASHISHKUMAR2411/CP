#include<iostream>
using namespace std;
int main()
{
    //! in this introduction we will discuss everything about the divide and conquer(DAC) algorithm and how it is used int computer science .
    //* 1. Introduction To DAC
    // *2. Algorithm under that .
    // *3. Recurrsion relation with DAC
    // *4. Problem using DAC technique .


    //* 1. Introduction To DAC
    // ? In this we have divide where we divide the problem into many other small problem we face and then we conquer by solving each of the problem step by step , then at the last we combine the sollution 

    // *2. Algorithm under that .
    // ? we are going to discuss teh algorithm with problem solving on them here we will just write their names ..
    // 1. Quicksort
    // 2. Merge Sort
    // 3. CLosest PAir of Points
    // 4. Strassen's Algoritm
    // 5.Cooley-Tukey FAst Fourier Transform . 
    // 6.KAratsuba Algoritm for fast multiplication 
    // 7. classical algorithm .


    //* 3. Decrease and COnquer (DAC) ex Binary search .
    //? Divide and conquer algoritm are :
    //* DAC(a,i,j)
    //* {
    //*     if(small(a,i,j))
    //*     {
    //*         return(solution(a,i,j));
    //*     }
    //*     else
    //*     {
    // *        m = divide(a,i,j);
    //*         b = DAC(a,i,mid);
    //*         c = DAC(a,mid+1,j);
    // *        d = combine(b,c);
    //*     }
    //*     return(d);
    //* }

    //? 3. Recurrsion relation with DAC
     

}