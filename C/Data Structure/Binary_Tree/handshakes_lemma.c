// In handshake lemma , we have different formula and propeerties which are included in the tree and its data structure 
// some of the properties are the :

// 1.In a K-array tree where the tree nodes either have 0 or k children . then number of leave nodes are always the
//          L = (k-1)*I + 1
// where the  I is the internal nodes  
  