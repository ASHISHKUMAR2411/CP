// in depth order traversal we have different order of traversal 
// 1. Inorder (left,root,right ) where first left element are shown and then root then all right element are shown in these
// 2. Preorder (root,left,right):where the root is first shown then left and then right
// 3.postorder (left,right,root): where the left is shown first , then right, then root

// lets do it then 