#include<bits/stdc++.h>
using namespace std;

// take input from a file
int main(){
    ifstream assemble("assembler.txt");
    ifstream opcode("opcode.txt");
    
    // assuming only one line is there.
    string assemblerline;
    string opcodeline;
    
    map<string, int> m;
    string word;
    
}